{
  "name": "{{MODULE_NAME}}",
  "format": "{{FORMAT}}",
  "path": "{{PATH}}",
  "trigger": "{{TRIGGER}}",
  "append_only": {{APPEND_ONLY}},
  "fields": [
    {{FIELDS_JSON_ARRAY}}
  ],
  "audit": {
    "completeness_rule": "{{COMPLETENESS_RULE}}",
    "missing_threshold_pct": {{MISSING_THRESHOLD_PCT}},
    "particular_audit": "{{PARTICULAR_AUDIT}}",
    "global_audit": "{{GLOBAL_AUDIT}}"
  },
  "added_by": "{{ADDED_BY}}",
  "confidence_pct": {{CONFIDENCE_PCT}},
  "non_removable_for_high_risk": {{NON_REMOVABLE_FOR_HIGH_RISK_OR_FALSE}}
}
