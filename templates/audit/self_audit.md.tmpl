# Self-audit report — {{system_name}}

> Auto-generated by the orchestrator's `self_audit` phase. Mirrors `audit/evaluation_session_0.json`. Consumed by Gate #2 to decide handoff.

**Session:** {{session_id}}
**Captured at:** {{TEMPORAL_NOW}}
**Audit scope:** {{audit_scope}} (e.g., scaffolding, milestone M1, final)

---

## 1 · Coverage of mandatory artifacts

| Artifact | Present | Bytes | sha256 |
|---|---|---|---|
{{mandatory_artifacts_table}}

**Coverage:** {{coverage_pct}}% (target 100%)

## 2 · prompt-architect rubric per emitted prompt

| Prompt | Tier | Tag count (target/actual) | Mandatory floor | Calibration | Portability | Bilingual | XML well-formed | Result |
|---|---|---|---|---|---|---|---|---|
{{prompt_architect_rubric_table}}

**First-pass rate:** {{audit_first_pass_pct}}% (target ≥85%)

## 3 · Calibration scan (P2)

- **Forbidden tokens regex:** `\b(best|always|never|guaranteed|certain|definitely|impossible)\b`
- **Hits in templates / emitted prompts:** {{calibration_hits}}
- **Missing-% scan results:** {{calibration_missing_pct}}
- **Verdict:** {{calibration_verdict}}

Detail (if hits >0):
{{calibration_detail}}

## 4 · Portability scan (P1)

- **Forbidden identifiers regex:** `\b(mcp__\w+|TodoWrite|SubagentType|Skill\(|EnterPlanMode|claude_code_specific)\b`
- **Hits in runtime artifacts:** {{portability_hits}}
- **Score:** {{portability_score}}/100 (-5 per Claude Code-specific dep, -10 per platform-only API)
- **Verdict:** {{portability_verdict}}

Detail (if hits >0):
{{portability_detail}}

## 5 · EU AI Act mapping completeness

- **Risk class:** {{eu_ai_act_risk}}
- **Rows required:** {{ai_act_rows_required}}
- **Rows emitted:** {{ai_act_rows_emitted}}
- **Completeness:** {{ai_act_completeness_pct}}%
- **Threshold for risk class:** {{ai_act_threshold_pct}}%
- **Verdict:** {{ai_act_verdict}}

## 6 · Living-doc invariant

- `tracking/project.json` exists + non-empty: {{tracking_project_present}}
- `tracking/sessions/0001_bootstrap/` exists with required files: {{session_0001_present}}
- `memory/MEMORY.md` exists: {{memory_present}}
- `audit/audit_sheet.xlsx` (or `.csv` fallback) exists: {{audit_sheet_present}}

## 7 · Portability fallbacks invoked

{{portability_fallbacks_log}}

## 8 · Parallelism fallbacks invoked

{{parallelism_fallbacks_log}}

## 9 · Calibration violations (intentional, documented)

{{calibration_intentional_violations}}

## 10 · Iterations / retries

| Prompt | Audit attempts | Escalated |
|---|---|---|
{{retries_table}}

## 11 · Overall verdict

- **Coverage:** {{coverage_verdict}}
- **prompt-architect rubric:** {{rubric_verdict}}
- **Calibration:** {{calibration_verdict}}
- **Portability:** {{portability_verdict}}
- **EU AI Act:** {{ai_act_verdict}}
- **Living-doc invariant:** {{living_doc_verdict}}

**Aggregate:** {{aggregate_verdict}} (PASS = all individual verdicts PASS or PASS_WITH_NOTE).

## 12 · Recommendations for next round

{{recommendations}}
