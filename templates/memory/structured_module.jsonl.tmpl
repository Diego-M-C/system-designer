{"_schema_ref": "memory_schema/modules/{{MODULE_NAME}}.json", "_initialised_at": "{{TEMPORAL_NOW}}", "_session_id": "{{SESSION_ID}}", "_note": "First line is the schema header. Subsequent lines are events; one JSON object per line. Append-only. Validate against the schema fields defined in memory_schema/modules/{{MODULE_NAME}}.json before writing."}
