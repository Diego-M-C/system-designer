# Improvement Proposal · {{PROPOSAL_ID}}

> Generated by phase 13.5 (`feedback_learning_loop`) when the pending-Y counter reached `learn_threshold = {{LEARN_THRESHOLD}}` (or the user typed `TRIGGER`). This is a **draft**; the proposal is **advisory** until phase 13.7 (`improvement_jury`) audits it and humans approve.
>
> Generated at: `{{TEMPORAL_NOW}}` · Session: `{{SESSION_ID}}` · System: `{{SYSTEM_NAME}}` · Generator version: `{{GENERATOR_VERSION}}`

## Scope

- Corrections in batch: `{{BATCH_SIZE}}`
- Severity distribution: `{{SEV_DIST}}`
- Category distribution: `{{CAT_DIST}}`
- Recurrence distribution: `{{REC_DIST}}`
- Mean classification confidence: `{{MEAN_CLASSIFICATION_CONF_PCT}}%`

## Batched corrections

| id | sev | cat | rec | feedback (≤160 chars) | proposed_action | conf% |
|---:|----|-----|-----|------------------------|------------------|------:|
{{ROWS_RENDERED}}

## Proposed system updates

> Each row in this section maps a correction (or a cluster) to a concrete file × change × confidence%. Phase 13.7 audits these.

| target_file | change_type (add | edit | remove) | summary | rationale | confidence% |
|---|---|---|---|---:|
{{UPDATES_RENDERED}}

## Risk register

> Minimum 3 risks with calibrated probability and impact (P2). Phase 13.7 mitigations expected.

| risk | probability% | impact (1-5) | mitigation |
|---|---:|---:|---|
{{RISKS_RENDERED}}

## Recommended owner

`{{RECOMMENDED_OWNER}}`

## Phase 13.7 invocation

After human acknowledgement of this proposal, the master orchestrator invokes `prompts/12_improvement_jury.md` with:

```
proposal_path = feedback_learning/improvement_proposal.md
proposal_id   = {{PROPOSAL_ID}}
session_id    = {{SESSION_ID}}
```

The jury (5 specialist auditors) returns an APPROVED / HITL_REQUIRED / REJECTED batch verdict. **No source change merges without that verdict and a human approval at the post-jury HITL gate.**
